CREATE TABLE bears (
        id INTEGER PRIMARY KEY,
                name TEXT,
                age INTEGER,
                gender INTEGER,
                color TEXT,
                temperament TEXT,
                alive BOOLEAN
            );
